"""Repository-level developer tools for animedex.

This package is intentionally not shipped as part of the published
distribution; it holds scripts that operate on the source tree, the
PyInstaller binary, or release artifacts. See ``tools/test_cli.py`` for
the post-build CLI smoke-test runner.
"""
